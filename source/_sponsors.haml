%table.sponsors-table
  %thead
    %tr
      %th.sponsor-logo
      %th Sponsor
      %th Total
      %th Donor since
  %tbody
    - sorted_sponsors(tier) do |member|
      %tr
        %td.sponsor-logo
          - if member['image']
            %img{src: member['image'], alt: member['name'], height: 30}
        %td
          - if member['website']
            %a{href: member['website'], title: member['name']}
              = member['name']
          - else
            = member['name']
        %td
          #{member['currency_symbol']}#{member['total_donated'] / 100}
        %td
          = sponsor_date(member['created_at'])

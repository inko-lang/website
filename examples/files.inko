# Reading from a file
import std::fs::file::ReadOnlyFile
import std::stdio::stdout

fn main {
  let file = try! ReadOnlyFile.new('README.md')
  let bytes = ByteArray.new

  try! file.read_all(bytes)

  stdout.print(bytes.to_string)
}
